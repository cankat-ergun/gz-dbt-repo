version: 2

models:
  - name: finance_days        # dosya adın: finance_days.sql
    description: Günlük finans özet tablosu
    columns:
      - name: date_date
        description: Her gün tam 1 satir olmali
        tests:
          - not_null
          - unique
